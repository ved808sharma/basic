pipeline {
    agent any
    stages {
        stage('Checkout SCM'){
            steps {
                echo 'checkout from SCM'
            }
        }
        stage('Build') {
            steps {
                echo 'build'
            }
        }
    }
}